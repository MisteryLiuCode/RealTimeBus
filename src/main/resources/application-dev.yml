spring:
  datasource:
    url: jdbc:mysql://101.43.145.108:3306/real_time_bus?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false
    username: real_time_bus
    password: l198923.
  redis:
    host: 101.43.145.108 # Redis服务器地址
    database: 1 # Redis数据库索引（默认为0）
    port: 6379 # Redis服务器连接端口
    password: l198923. # Redis服务器连接密码（默认为空）
    timeout: 15000ms # 连接超时时间（毫秒）

logging:
  level:
    root: info
    com.macro.mall: debug